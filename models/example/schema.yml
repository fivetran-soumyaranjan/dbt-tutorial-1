
version: 2

models:
  - name: my_first_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null

  - name: my_second_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null

  - name: my_third_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null

  - name: my_fourth_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null          

  - name: s3_source_model
    description: "A starter dbt model"
    columns:
      - name: _line
        description: "The primary key for this table"
        tests:
          - unique
          - not_null           

  - name: s3_source_model_dup
    description: "A starter dbt model"
    columns:
      - name: _line
        description: "The primary key for this table"
        tests:
          - unique
          - not_null           